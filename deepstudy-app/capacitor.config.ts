import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.deepstudy.focus',
  appName: 'DeepStudy',
  webDir: 'dist',
  android: {
    backgroundColor: '#f4f6fb',
  },
}

export default config
